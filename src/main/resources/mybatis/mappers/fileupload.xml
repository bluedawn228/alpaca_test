<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
							"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mulcam.blob.dao.FileUploadDAO">
	<insert id="uploadFile" parameterType="FileVO"> <!-- vo 파라미터타입 다 소문자 알리야스때문에 -->
		<![CDATA[
		insert into tbl_pf_img
		(pf_img_id, file_name, file_size, file_content_type, file_upload_date, file_data, regdate, updatedate)
		values(pf_img_sq.nextval, #{fileName}, #{fileSize}, #{fileContentType}, current_timestamp, #{fileData}, sysdate, sysdate)
		]]>
	</insert>
</mapper>
