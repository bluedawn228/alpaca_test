<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
							"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mulcam.alpaca.dao.ReviewDAO">
	
	<insert id="selectRImgNextval">
		<![CDATA[
		select r_img_test_sq.nextval from dual
		]]>	
	</insert>

	<insert id="insertRImg" parameterType="FileVO">
		<![CDATA[
		insert into tbl_r_img_test
		(r_img_id, file_name, file_size, file_content_type, file_upload_date, file_data, regdate, updatedate)
		values(#{r_img_id}, #{file_name}, #{file_size}, #{file_content_type}, current_timestamp, #{file_data}, sysdate, sysdate)
		]]>	
	</insert>
	
	<insert id="insertReviewTemp" parameterType="ReviewVO">
		<![CDATA[
		insert into tbl_review_test
		(review_id, r_img_id, email, pos, company, title, content, comm_cnt, like_cnt, view_cnt, regdate, updatedate, temp)
		values(review_sq.nextval, #{r_img_id}, e@naver.com, #{pos}, #{company}, #{title}, #{content}, #{comm_cnt}, #{like_cnt}, #{view_cnt}, sysdate, sysdate, 1)
		]]>	
	</insert>
	
	<insert id="insertReview" parameterType="ReviewVO">
		<![CDATA[
		insert into tbl_review_test 
		(review_id, r_img_id, email, pos, company, title, content, comm_cnt, like_cnt, view_cnt, regdate, updatedate, temp)
		values(review_sq.nextval, #{r_img_id}, e@naver.com, #{pos}, #{company}, #{title}, #{content}, #{comm_cnt}, #{like_cnt}, #{view_cnt}, sysdate, sysdate, 0)
		]]>	
	</insert>
	
	
</mapper>
